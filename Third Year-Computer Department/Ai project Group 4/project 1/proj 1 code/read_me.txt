- install the osmnx library using --- > pip install osmnx , write this in terminal 
- install the matplot library using --- > pip install matplotlib , also write this in terminal
- install graphiz from the extensions of vscode (the first 3 extensions) to be able to see the tree of explored nodes as a file in .dot       
  format at end
- open the folder using cmd or gitbash using code . command or open folder not file only from vscode
- to get good result in comparison between time and euclidean distance heuristic of greedy algorithm use capital_federal_coords on line 10 
  instead of qalyob cords 
- enjoy ^_^